{
  "name": "ponder",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev --schema ponder",
    "start": "ponder start --schema ponder",
    "codegen": "ponder codegen",
    "lint": "eslint .",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "ponder": "^0.17.6",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "eslint": "^9.31.0",
    "typescript": "^5.8.2"
  }
}
