{
  "name": "contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "lint": "forge fmt --check",
    "format": "forge fmt",
    "deploy": "./deploy.sh",
    "deploy:fresh": "./deploy.sh --fresh",
    "explorer": "./otterscan/start.sh",
    "explorer:fresh": "./otterscan/start.sh --fresh",
    "explorer:stop": "./otterscan/stop.sh"
  }
}
